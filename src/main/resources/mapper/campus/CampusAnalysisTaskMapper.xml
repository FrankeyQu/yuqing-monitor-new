<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.stonedt.intelligence.dao.campus.CampusAnalysisTaskDao">

    <resultMap id="CampusAnalysisTaskResultMap" type="com.stonedt.intelligence.entity.campus.CampusAnalysisTask">
        <id column="id" property="id"/>
        <result column="analysis_task_id" property="analysisTaskId"/>
        <result column="object_type" property="objectType"/>
        <result column="object_id" property="objectId"/>
        <result column="analysis_type" property="analysisType"/>
        <result column="task_status" property="taskStatus"/>
        <result column="request_payload" property="requestPayload"/>
        <result column="model_provider" property="modelProvider"/>
        <result column="model_name" property="modelName"/>
        <result column="error_message" property="errorMessage"/>
        <result column="deleted" property="deleted"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="update_user_id" property="updateUserId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="BaseColumns">
        id, analysis_task_id, object_type, object_id, analysis_type, task_status,
        request_payload, model_provider, model_name, error_message, deleted,
        create_user_id, update_user_id, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.stonedt.intelligence.entity.campus.CampusAnalysisTask">
        INSERT INTO campus_analysis_task (
            analysis_task_id, object_type, object_id, analysis_type, task_status,
            request_payload, model_provider, model_name, error_message, deleted,
            create_user_id, update_user_id
        ) VALUES (
            #{analysisTaskId}, #{objectType}, #{objectId}, #{analysisType}, #{taskStatus},
            #{requestPayload}, #{modelProvider}, #{modelName}, #{errorMessage}, #{deleted},
            #{createUserId}, #{updateUserId}
        )
    </insert>

    <update id="updateStatus">
        UPDATE campus_analysis_task
        SET task_status = #{taskStatus},
            error_message = #{errorMessage},
            update_user_id = #{updateUserId}
        WHERE analysis_task_id = #{analysisTaskId}
          AND deleted = 0
    </update>

    <select id="selectByTaskId" resultMap="CampusAnalysisTaskResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM campus_analysis_task
        WHERE analysis_task_id = #{analysisTaskId}
          AND deleted = 0
        LIMIT 1
    </select>

    <select id="list" resultMap="CampusAnalysisTaskResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM campus_analysis_task
        WHERE deleted = 0
        <if test="objectType != null and objectType != ''">
            AND object_type = #{objectType}
        </if>
        <if test="objectId != null">
            AND object_id = #{objectId}
        </if>
        <if test="analysisType != null and analysisType != ''">
            AND analysis_type = #{analysisType}
        </if>
        <if test="taskStatus != null and taskStatus != ''">
            AND task_status = #{taskStatus}
        </if>
        ORDER BY id DESC
    </select>
</mapper>

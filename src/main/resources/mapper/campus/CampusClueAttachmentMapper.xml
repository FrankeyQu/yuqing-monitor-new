<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.stonedt.intelligence.dao.campus.CampusClueAttachmentDao">

    <resultMap id="CampusClueAttachmentResultMap" type="com.stonedt.intelligence.entity.campus.CampusClueAttachment">
        <id column="id" property="id"/>
        <result column="attachment_id" property="attachmentId"/>
        <result column="clue_id" property="clueId"/>
        <result column="file_name" property="fileName"/>
        <result column="file_path" property="filePath"/>
        <result column="file_type" property="fileType"/>
        <result column="file_size" property="fileSize"/>
        <result column="deleted" property="deleted"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.stonedt.intelligence.entity.campus.CampusClueAttachment">
        INSERT INTO campus_clue_attachment (
            attachment_id, clue_id, file_name, file_path, file_type, file_size, deleted, create_user_id
        ) VALUES (
            #{attachmentId}, #{clueId}, #{fileName}, #{filePath}, #{fileType}, #{fileSize}, #{deleted}, #{createUserId}
        )
    </insert>

    <update id="logicalDelete">
        UPDATE campus_clue_attachment
        SET deleted = 1
        WHERE attachment_id = #{attachmentId}
          AND clue_id = #{clueId}
          AND deleted = 0
    </update>

    <select id="listByClueId" resultMap="CampusClueAttachmentResultMap">
        SELECT id, attachment_id, clue_id, file_name, file_path, file_type, file_size,
               deleted, create_user_id, create_time
        FROM campus_clue_attachment
        WHERE clue_id = #{clueId}
          AND deleted = 0
        ORDER BY id ASC
    </select>
</mapper>

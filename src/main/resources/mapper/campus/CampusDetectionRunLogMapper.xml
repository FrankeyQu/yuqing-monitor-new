<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.stonedt.intelligence.dao.campus.CampusDetectionRunLogDao">

    <resultMap id="CampusDetectionRunLogResultMap" type="com.stonedt.intelligence.entity.campus.CampusDetectionRunLog">
        <id column="id" property="id"/>
        <result column="run_log_id" property="runLogId"/>
        <result column="detection_task_id" property="detectionTaskId"/>
        <result column="run_status" property="runStatus"/>
        <result column="trigger_type" property="triggerType"/>
        <result column="trigger_object_type" property="triggerObjectType"/>
        <result column="trigger_object_id" property="triggerObjectId"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
        <result column="scanned_count" property="scannedCount"/>
        <result column="hit_count" property="hitCount"/>
        <result column="alert_count" property="alertCount"/>
        <result column="error_message" property="errorMessage"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="BaseColumns">
        id, run_log_id, detection_task_id, run_status, trigger_type, trigger_object_type,
        trigger_object_id, start_time, end_time,
        scanned_count, hit_count, alert_count, error_message, create_user_id,
        create_time
    </sql>

    <insert id="insert" parameterType="com.stonedt.intelligence.entity.campus.CampusDetectionRunLog">
        INSERT INTO campus_detection_run_log (
            run_log_id, detection_task_id, run_status, trigger_type, trigger_object_type,
            trigger_object_id, start_time, scanned_count,
            hit_count, alert_count, error_message, create_user_id
        ) VALUES (
            #{runLogId}, #{detectionTaskId}, #{runStatus}, #{triggerType}, #{triggerObjectType},
            #{triggerObjectId}, #{startTime}, #{scannedCount},
            #{hitCount}, #{alertCount}, #{errorMessage}, #{createUserId}
        )
    </insert>

    <update id="finish">
        UPDATE campus_detection_run_log
        SET run_status = #{runStatus},
            end_time = NOW(),
            scanned_count = #{scannedCount},
            hit_count = #{hitCount},
            alert_count = #{alertCount},
            error_message = #{errorMessage}
        WHERE run_log_id = #{runLogId}
    </update>

    <select id="selectByRunLogId" resultMap="CampusDetectionRunLogResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM campus_detection_run_log
        WHERE run_log_id = #{runLogId}
        LIMIT 1
    </select>

    <select id="listByTaskId" resultMap="CampusDetectionRunLogResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM campus_detection_run_log
        WHERE detection_task_id = #{detectionTaskId}
        ORDER BY start_time DESC, id DESC
    </select>
</mapper>

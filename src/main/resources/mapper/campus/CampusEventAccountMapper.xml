<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.stonedt.intelligence.dao.campus.CampusEventAccountDao">

    <resultMap id="CampusEventAccountResultMap" type="com.stonedt.intelligence.entity.campus.CampusEventAccount">
        <id column="id" property="id"/>
        <result column="relation_id" property="relationId"/>
        <result column="event_id" property="eventId"/>
        <result column="account_id" property="accountId"/>
        <result column="deleted" property="deleted"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.stonedt.intelligence.entity.campus.CampusEventAccount">
        INSERT INTO campus_event_account (relation_id, event_id, account_id, deleted, create_user_id)
        VALUES (#{relationId}, #{eventId}, #{accountId}, #{deleted}, #{createUserId})
    </insert>

    <select id="listByEventId" resultMap="CampusEventAccountResultMap">
        SELECT id, relation_id, event_id, account_id, deleted, create_user_id, create_time
        FROM campus_event_account
        WHERE event_id = #{eventId}
          AND deleted = 0
        ORDER BY id ASC
    </select>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.stonedt.intelligence.dao.campus.CampusEventClueDao">

    <resultMap id="CampusEventClueResultMap" type="com.stonedt.intelligence.entity.campus.CampusEventClue">
        <id column="id" property="id"/>
        <result column="relation_id" property="relationId"/>
        <result column="event_id" property="eventId"/>
        <result column="clue_id" property="clueId"/>
        <result column="deleted" property="deleted"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.stonedt.intelligence.entity.campus.CampusEventClue">
        INSERT INTO campus_event_clue (relation_id, event_id, clue_id, deleted, create_user_id)
        VALUES (#{relationId}, #{eventId}, #{clueId}, #{deleted}, #{createUserId})
    </insert>

    <select id="selectByEventAndClue" resultMap="CampusEventClueResultMap">
        SELECT id, relation_id, event_id, clue_id, deleted, create_user_id, create_time
        FROM campus_event_clue
        WHERE event_id = #{eventId}
          AND clue_id = #{clueId}
          AND deleted = 0
        LIMIT 1
    </select>

    <select id="listByEventId" resultMap="CampusEventClueResultMap">
        SELECT id, relation_id, event_id, clue_id, deleted, create_user_id, create_time
        FROM campus_event_clue
        WHERE event_id = #{eventId}
          AND deleted = 0
        ORDER BY id ASC
    </select>
</mapper>

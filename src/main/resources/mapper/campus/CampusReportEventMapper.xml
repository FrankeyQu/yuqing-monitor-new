<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.stonedt.intelligence.dao.campus.CampusReportEventDao">

    <resultMap id="CampusReportEventResultMap" type="com.stonedt.intelligence.entity.campus.CampusReportEvent">
        <id column="id" property="id"/>
        <result column="relation_id" property="relationId"/>
        <result column="report_id" property="reportId"/>
        <result column="event_id" property="eventId"/>
        <result column="deleted" property="deleted"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.stonedt.intelligence.entity.campus.CampusReportEvent">
        INSERT INTO campus_report_event (
            relation_id, report_id, event_id, deleted, create_user_id
        ) VALUES (
            #{relationId}, #{reportId}, #{eventId}, #{deleted}, #{createUserId}
        )
    </insert>

    <update id="logicalDeleteByReportId">
        UPDATE campus_report_event
        SET deleted = 1
        WHERE report_id = #{reportId}
          AND deleted = 0
    </update>

    <select id="listByReportId" resultMap="CampusReportEventResultMap">
        SELECT id, relation_id, report_id, event_id, deleted, create_user_id, create_time
        FROM campus_report_event
        WHERE report_id = #{reportId}
          AND deleted = 0
        ORDER BY id DESC
    </select>
</mapper>
